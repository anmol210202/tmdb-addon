{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "src": "/configure/(.*)", "dest": "/$1" },
    { "src": "/configure", "dest": "/index.html" }
  ]
}
